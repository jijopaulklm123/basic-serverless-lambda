name: Staging Auto-PR
on:
  push:
    branches: ['dev']

jobs:
  pull-request:
    name: Open PR to main
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
      name: checkout

    - uses: repo-sync/pull-request@v2
      name: pull-request
      with:
        destination_branch: "main"
        pr_title: "Pulling ${{ github.ref }} into main"
        pr_body: "👑 *An automated PR*"
        pr_reviewer: "jijopaulklm123"
        github_token: ${{ secrets.GITHUB_TOKEN }}
    - uses: dhollerbach/github-action-send-message-to-ms-teams@1.0.10
      name: Notify  
      with:
        webhook: 'https://alamy.webhook.office.com/webhookb2/b88610d1-766e-4b67-b4bd-4aff7a2be34e@a34ab451-2e45-46ab-812b-05f284201608/IncomingWebhook/93543f055c7548d49b07a4a4f8080299/f6effabe-7f9b-41fe-bbbf-cd298892bf34'
        message: 'Got My Message!!!!!!!!!!!'        



